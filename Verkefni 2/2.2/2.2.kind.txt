var wheelDiameter = 10; //10 cm used as an example

driveForward() //Void that makes sure that the robot drives straight
driveBackwards() //Void that makes sure that the robot drives straight in reverse

RightEncoder() = 0;
LeftEncoder = 0;
int a = 1

//50 cm = 10*5
//360*5=1800
for(i=1; i < 5; i = i + 1){
  While(RigthEncoder && LeftEncoder < 1800)
  {
    DriveForward()
  }

  While(RightEncoder && LeftEncoder < 1800)
  {
    DriveBackwards()
  }
}
